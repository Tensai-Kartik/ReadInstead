{
  "name": "readinstead-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "ReadInstead — AI-Powered Video Lecture Summarizer and Study Guide Workspace",
  "scripts": {
    "dev": "concurrently -k -n \"frontend,backend\" -c \"cyan.bold,green.bold\" \"npm run dev --prefix frontend\" \"python -m uvicorn main:app --reload --port 8000 --app-dir backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python -m uvicorn main:app --reload --port 8000 --app-dir backend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "python -m uvicorn main:app --reload --port 8000 --app-dir backend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "keywords": [
    "ai",
    "youtube-summarizer",
    "fastapi",
    "react",
    "supabase",
    "rag",
    "groq"
  ],
  "author": "Tensai-Kartik",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
